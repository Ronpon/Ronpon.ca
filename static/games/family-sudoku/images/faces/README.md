# Family face images

Add approved, square, same-origin crops here as `face-1.webp` through `face-9.webp`.
Missing files are intentional and become numbered/short-label fallbacks at runtime.
These public static files should be low resolution and privacy-approved.
